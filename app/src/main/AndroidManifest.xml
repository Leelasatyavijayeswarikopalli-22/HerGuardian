<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.herguardian.app">
    <!-- ── To access exact Location to send to emergency contacts ── -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- ── Microphone for voice phrase detection ── -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ── Internet for WebView + API calls ── -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ── Foreground service keeps mic alive when screen is off ── -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission

        android:name=
            "android.permission.ACCESS_BACKGROUND_LOCATION"

        />
    <uses-permission

        android:name=
            "android.permission.FOREGROUND_SERVICE_LOCATION"

        />
    <!-- ── WakeLock keeps CPU running when screen is off ── -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ── Restart service after phone reboot ── -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ── Vibrate on SOS ── -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:allowBackup="true"
        android:label="HerGuardian"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="false">

        <!-- ── Main Activity (WebView) ── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Voice Listener Service (screen-off detection) ── -->
        <service
            android:name=".service.VoiceListenerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- ── Boot Receiver (restart service after reboot) ── -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>